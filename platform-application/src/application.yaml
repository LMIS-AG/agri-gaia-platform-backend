spring:
  datasource:
    url: jdbc:mariadb://172.30.84.196:3306/agrigaia_platform_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: agrigaia
    password: agrigaia
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://account.platform.agri-gaia.com/auth/realms/agri-gaia

logging:
  level:
    de.agrigaia.platform: TRACE

agrigaia-platform:
  allowed-origins: '*'
  client-id: platform
